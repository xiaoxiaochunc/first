#include<stdio.h>
int main()
{
	int x,digit,ret=0;

	scanf_s("%d", &x);
	while(x>0) {
		digit = x % 10;
		printf("%d", digit);
		ret = ret * 10 + digit;
		//printf("x=%d,ret=%d,digit=%d\n", x, ret, digit);
		x /= 10;

	}
	//printf("ret=%d\n", ret);
	return 0;
}
